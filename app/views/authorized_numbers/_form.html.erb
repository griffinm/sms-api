<%= form_for(@authorized_number, html: { class: 'form-horizontal' }) do |f| %>
  <% if @authorized_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authorized_number.errors.count, "error") %> prohibited this authorized_number from being saved:</h2>

      <ul>
      <% @authorized_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :phone_number, class: 'col-sm-3 control-label' %>
    <div class="col-sm-4">
      <%= f.text_field :phone_number, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-4">
      <button type="submit" class="btn btn-default">Save</button>
    </div>
  </div>

<% end %>
